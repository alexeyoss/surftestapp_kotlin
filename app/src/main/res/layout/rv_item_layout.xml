<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/rv_layout">

    <ImageView
        android:id="@+id/poster"
        style="@style/rv_poster"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tv_filmName"
        style="@style/rv_film_name"
        android:text="@string/text_example"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@+id/poster"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tv_desc"
        style="@style/rv_item_desc"
        android:text="@string/text_example"
        app:layout_constraintBottom_toTopOf="@+id/ic_heart"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@+id/poster"
        app:layout_constraintTop_toBottomOf="@+id/tv_filmName" />


    <ImageView
        android:id="@+id/ic_calendar"
        style="@style/rv_ic_calendar"
        android:src="@drawable/ic_calendar"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@+id/poster"
        tools:ignore="ImageContrastCheck" />

    <TextView
        android:id="@+id/tv_date"
        style="@style/rv_date"
        android:text="@string/date_example"
        app:layout_constraintBottom_toBottomOf="@+id/ic_calendar"
        app:layout_constraintEnd_toStartOf="@+id/ic_heart"
        app:layout_constraintStart_toEndOf="@+id/ic_calendar"
        app:layout_constraintTop_toTopOf="@+id/ic_calendar" />

    <ImageView
        android:id="@+id/ic_heart"
        style="@style/rv_ic_heart"
        android:src="@drawable/ic_heart"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />


</androidx.constraintlayout.widget.ConstraintLayout>