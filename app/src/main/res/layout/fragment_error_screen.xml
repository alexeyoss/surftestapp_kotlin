<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".fragments.ErrorScreenFragment">

    <ImageView
        android:id="@+id/iv_error"
        style="@style/error_fragment_ic_attention"
        android:src="@drawable/ic_attention"
        app:layout_constraintBottom_toTopOf="@+id/refreshImageButton"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tv_error"
        style="@style/error_fragment_text"
        android:text="@string/error_text"
        app:layout_constraintEnd_toEndOf="@+id/iv_error"
        app:layout_constraintStart_toStartOf="@+id/iv_error"
        app:layout_constraintTop_toBottomOf="@+id/iv_error" />

    <ImageView
        android:id="@+id/refreshImageButton"
        style="@style/error_fragment_btn"
        android:src="@drawable/ic_refresh"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />


</androidx.constraintlayout.widget.ConstraintLayout>